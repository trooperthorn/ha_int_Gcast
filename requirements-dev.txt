# Linting and formatting
ruff==0.16.8

# Type checking
mypy==2.3.1
homeassistant-stubs==2026.9.3

# Home Assistant core and the test harness (the harness pins core 2026.9.3)
pytest-homeassistant-custom-component==0.13.366
pytest-cov==7.1.0

# Integration dependency, pinned exactly as core's manifest pins it
PyChromecast==14.0.10

# Test-only: core tts (which the ported cast tests set up) imports these
mutagen==1.48.1
ha-ffmpeg==3.2.2

# Test-only: importing homeassistant.components.dhcp (the public source of the
# IP-to-MAC map) pulls these in; pinned exactly as core's dhcp manifest pins them
aiodhcpwatcher==1.2.7
aiodiscover==3.3.2
